// import  React, { useState } from 'react'

function A(props){
    // const [, setCountA] = useState(props.valueA);
    // const [, setCountB] = useState(props.valueB);
    return(
        <> 
        {/* <h2>A.js Buttons</h2>
        <p>Value of A on Increasing by 1: {props.countA}</p>
        <button onClick={() =>setCountA(prev=>prev+1)}>Increment A</button>

        <p>Value of B on Increasing by 1: {props.countB}</p>
        <button onClick={() =>setCountB(prev=>prev+1)}>Increment B</button> */}
        </>
    )
}

export default A;